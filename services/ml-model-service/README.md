# ML model service

Placeholder for the recommendation / ML inference service. Add application code, dependencies, and a `Dockerfile` here when you implement the service.

The API platform lives in `../platform/` and can call this service over HTTP using the Docker Compose service name (for example `http://ml-model-service:<port>`) once this service is wired into `docker-compose.yml` at the repository root.
